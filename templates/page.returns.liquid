<div class="Container Container--narrow">
  <div class="Main__header">
    <h1>{{ page.title }}</h1>
  </div>

  <div class="Main__content">
    <div>{{page.content}}</div>
    {% section 'page-template-returns' %}
  </div>
</div>